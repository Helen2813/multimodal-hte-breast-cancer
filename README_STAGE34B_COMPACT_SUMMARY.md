# Paper A Stage 34B — compact confirmatory summary

Extract into the project root and run:

```powershell
.\run_stage34b_compact_confirmatory_summary.ps1
```

This stage reruns no simulation. It verifies the completed Stage 34 outputs and
prints only the top-level decision, key true-zero and empirical-sequencing
results, empirical anchor checks, and any failed locked gates.

Upload the single Stage 34B log after completion.
